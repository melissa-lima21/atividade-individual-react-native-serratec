import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        backgroundColor: '#bf065f', 
        padding: 10,
        borderRadius: 40,
        alignItems: 'center',
        marginTop: 20,
        width: '100%'
    },
    buttonText: {
        color: '#FFF',
        fontSize: 17,
        fontWeight: 'bold'
    },
});